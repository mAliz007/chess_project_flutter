// File: lib/screens/single_player_page.dart

import 'package:flutter/material.dart';

class SinglePlayerPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Single Player')),
      body: Center(child: Text('This is the Single Player Page')),
    );
  }
}
